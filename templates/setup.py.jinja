import setuptools

with open("README.md", "r") as fh:
    long_description = fh.read()

from litex.data.{{ type }}.{{ name }} import version_str

setuptools.setup(
    name="{{ repo }}",
    version=version_str,
    author="LiteX Authors",
    author_email="litex@googlegroups.com",
    description="Python module containing data files for using the {{ human_name }} {{ type }} with LiteX.",
    long_description=long_description,
    long_description_content_type="text/markdown",
    url="https://github.com/litex-hub/{{ repo }}",
    classifiers=[
        "Programming Language :: Python :: 3",
        "{{ license }}",
        "Operating System :: OS Independent",
    ],
    python_requires='>=3.5',
    zip_safe=False,
    packages=setuptools.find_packages(),
    package_data={'litex.data.{{ type }}.{{ name }}': ['litex/data/{{ type }}/{{ name }}/{{ contents }}/**']},
    include_package_data=True,
)
